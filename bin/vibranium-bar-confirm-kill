#!/usr/bin/env bash

STATE_FILE="$XDG_RUNTIME_DIR/vibranium-core-force-kill.confirm"
ICON="\\udb84\\udf98"
TOOLTIP="Repeat shortcut to confirm kill"

if [[ -f "$STATE_FILE" ]]; then
    printf '{"text": "%s", "tooltip": "%s"}' "$ICON" "$TOOLTIP"
else
    printf '{"text": ""}'
fi
