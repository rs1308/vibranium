#!/usr/bin/env bash

# Name of the terminal window
TITLE="dropdown-terminal"

# Check if it's already running
if hyprctl clients | grep -q "$TITLE"; then
    # If it exists, kill it
    pkill -f "$TITLE"
else
    # Otherwise, spawn it
    alacritty --class dropdown-terminal --title "$TITLE" &
fi
